extend-exclude = [
  "scripts/**",
  "quarantine/**",
  "agents/**",
  "tests/**"
]

[lint]
ignore = ["E701","E702"]  # multiple statements per line (legacy)

[lint.per-file-ignores]
"tests/**" = ["F841"]  # allow unused vars in tests

# build / runtime debris
venv/
.env
.venv/
__pycache__/
*.pyc
.coverage
htmlcov/

# large/generated outputs (keep the demo mp4 tracked)
outputs/**
!outputs/dashboard/**
!outputs/summaries/**

# logs (keep repo-friendly summaries only)
logs/**

# demos (track the mp4, ignore temp audio)
docs/demos/*.wav

# coverage / pytest
coverage.xml
.pytest_cache/
# Python
# local outputs
phase_manifest.json
# editor/OS
.DS_Store
# local backups
tools/*.bak*

.coverage*


backups/

# --- allow MAGIC receipts under outputs/reports ---
!outputs/reports/*.tsv
!outputs/reports/*.md
!outputs/reports/restore_smoke_ok.txt
!outputs/reports/nightly_*.txt
!outputs/reports/post_mortem_*.md
# --- allow MAGIC receipts under outputs/reports ---
!outputs/reports/*.tsv
!outputs/reports/*.md
!outputs/reports/*.txt
!outputs/logs/notion_sync/*.txt
# --- Keep reports (whitelist) ---
!outputs/reports/
!outputs/reports/**
# Reports & heavy outputs
outputs/reports/**/*.json
outputs/reports/**/*.tsv
outputs/reports/**/*.txt
outputs/reports/**/*.log
clean_env/
env/
.venv/
venv/
outputs/cache/
clean_env/
env/
.venv/
venv/
outputs/cache/

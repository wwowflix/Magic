# build / runtime debris
venv/
.env
.venv/
__pycache__/
*.pyc
.coverage
htmlcov/

# large/generated outputs (keep the demo mp4 tracked)
outputs/**
!outputs/dashboard/**
!outputs/summaries/**

# logs (keep repo-friendly summaries only)
logs/**

# demos (track the mp4, ignore temp audio)
docs/demos/*.wav

# coverage / pytest
coverage.xml
.pytest_cache/
# Python
# local outputs
phase_manifest.json
# editor/OS
.DS_Store
# local backups
tools/*.bak*

# local helper files
PR_BODY.md
ci_tests.log
.pre-commit-config.yaml.bak_*


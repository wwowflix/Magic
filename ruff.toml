[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [
  "scripts",
  "quarantine",
  "outputs",
  "wheels",
  "venv",
  ".venv",
  "**/__pycache__",
]
# If duplicate-dict-keys are noisy, you can un-comment:
# ignore = ["F601"]

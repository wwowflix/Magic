[pytest]
addopts = -q --disable-warnings
norecursedirs =
    backups
    outputs
    .git
    venv
    tools
testpaths =
    tests
# Week 5 safe set: skip the heavy / broken ones
# (we'll re-enable in Week 6 when pandas imports are clean)
python_files = test_*.py

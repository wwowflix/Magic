[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
# Optional: don't recurse into non-test dirs
norecursedirs = scripts outputs .venv venv .git

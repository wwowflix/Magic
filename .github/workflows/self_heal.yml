name: self-heal
on:
  workflow_dispatch:
jobs:
  ci:
    if: false   # keep the job defined but never runs unless you later re-enable
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled for PRs"

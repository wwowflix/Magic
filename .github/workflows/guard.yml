name: guard-proof
on:
  push:
    branches: [ main, chore/precommit-rescue ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
          Write-Host "  MAGIC_GUARD_BLOCK: .artifacts/secret.txt"
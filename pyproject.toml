[tool.ruff]
target-version = "py313"
line-length = 100

[tool.pytest.ini_options]
addopts = "-q"

[tool.coverage.run]
source = ["."]
branch = true

[tool.coverage.report]
exclude_lines = ["pragma: no cover"]

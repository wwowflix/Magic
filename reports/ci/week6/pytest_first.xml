<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="1" skipped="1" tests="214" time="109.729" timestamp="2025-11-08T02:30:38.991917+05:30" hostname="LAPTOP-OQMBU0B0"><testcase classname="tests.invariants.test_no_hardcoded_drives" name="test_no_hardcoded_drives_in_phase11_python" time="0.111" /><testcase classname="tests.phase11.test_11C_behavioral_verification" name="test_import_11c" time="0.015" /><testcase classname="tests.phase11.test_11C_behavioral_verification" name="test_has_config_template" time="0.001" /><testcase classname="tests.phase11.test_11D_api_callchain" name="test_import_11d" time="0.007" /><testcase classname="tests.phase11.test_11D_api_callchain" name="test_config_exists" time="0.002" /><testcase classname="tests.phase11.test_11D_api_callchain_integrity" name="test_import_11d" time="0.012" /><testcase classname="tests.phase11.test_11D_api_callchain_integrity" name="test_has_api_config" time="0.000" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_allowed_shims_import[scripts._asarray]" time="0.019" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_allowed_shims_import[scripts.multiarray]" time="0.019" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_allowed_shims_import[scripts._add_newdocs]" time="0.044" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_allowed_shims_import[scripts._add_newdocs_scalars]" time="0.017" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_forbidden_shadow_names_blocked[scripts.overrides]" time="0.048" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_forbidden_shadow_names_blocked[scripts.fromnumeric]" time="0.001" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_forbidden_shadow_names_blocked[scripts._methods]" time="0.001" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_forbidden_shadow_names_blocked[scripts._dtype]" time="0.002" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_forbidden_shadow_names_blocked[scripts._type_aliases]" time="0.001" /><testcase classname="tests.policy.test_numpy_shadow_policy" name="test_forbidden_shadow_names_blocked[scripts._ufunc_config]" time="0.001" /><testcase classname="tests.smoke.test_fonttools_guard" name="test_fonttools_has_featureparams_size" time="0.004" /><testcase classname="tests.smoke.test_no_local_otTables" name="test_no_local_otTables_shadowing" time="0.001" /><testcase classname="tests.smoke.test_phase11AA_ok" name="test_phase11_ok" time="0.466" /><testcase classname="tests.smoke.test_phase11AB_ok" name="test_phase11_ok" time="0.556" /><testcase classname="tests.smoke.test_phase11A_ok" name="test_phase11_ok" time="1.397" /><testcase classname="tests.smoke.test_phase11B_ok" name="test_phase11_ok" time="0.695" /><testcase classname="tests.smoke.test_phase11C_ok" name="test_phase11_ok" time="0.128" /><testcase classname="tests.smoke.test_phase11E_ok" name="test_phase11_ok" time="1.090" /><testcase classname="tests.smoke.test_phase11F_ok" name="test_phase11_ok" time="1.194" /><testcase classname="tests.smoke.test_phase11G_ok" name="test_phase11_ok" time="0.329" /><testcase classname="tests.smoke.test_phase11H_ok" name="test_phase11_ok" time="0.898" /><testcase classname="tests.smoke.test_phase11I_ok" name="test_phase11_ok" time="1.430" /><testcase classname="tests.smoke.test_phase11J_ok" name="test_phase11_ok" time="0.700" /><testcase classname="tests.smoke.test_phase11K_ok" name="test_phase11_ok" time="0.556" /><testcase classname="tests.smoke.test_phase11L_ok" name="test_phase11_ok" time="0.592" /><testcase classname="tests.smoke.test_phase11M_ok" name="test_phase11_ok" time="0.517" /><testcase classname="tests.smoke.test_phase11N_ok" name="test_phase11_ok" time="0.642" /><testcase classname="tests.smoke.test_phase11O_ok" name="test_phase11_ok" time="0.565" /><testcase classname="tests.smoke.test_phase11P_ok" name="test_phase11_ok" time="0.471" /><testcase classname="tests.smoke.test_phase11Q_ok" name="test_phase11_ok" time="0.742" /><testcase classname="tests.smoke.test_phase11R_ok" name="test_phase11_ok" time="0.816" /><testcase classname="tests.smoke.test_phase11S_ok" name="test_phase11_ok" time="0.747" /><testcase classname="tests.smoke.test_phase11T_ok" name="test_phase11_ok" time="0.657" /><testcase classname="tests.smoke.test_phase11U_ok" name="test_phase11_ok" time="0.513" /><testcase classname="tests.smoke.test_phase11V_ok" name="test_phase11_ok" time="0.606" /><testcase classname="tests.smoke.test_phase11W_ok" name="test_phase11_ok" time="1.051" /><testcase classname="tests.smoke.test_phase11X_ok" name="test_phase11_ok" time="0.982" /><testcase classname="tests.smoke.test_phase11Y_ok" name="test_phase11_ok" time="1.078" /><testcase classname="tests.smoke.test_phase11Z_ok" name="test_phase11_ok" time="0.933" /><testcase classname="tests.smoke.test_phase11_param_ok" name="test_phase11_ok[NOTSET]" time="0.000"><skipped type="pytest.skip" message="got empty parameter set for (mod)">E:\MAGIC\tests\smoke\test_phase11_param_ok.py:16: got empty parameter set for (mod)</skipped></testcase><testcase classname="tests.smoke.test_smoke_scripts_BitmapGlyphMetrics" name="test_import_scripts_BitmapGlyphMetrics" time="0.332" /><testcase classname="tests.smoke.test_smoke_scripts_Blocks" name="test_import_scripts_Blocks" time="0.014" /><testcase classname="tests.smoke.test_smoke_scripts_CFF2ToCFF" name="test_import_scripts_CFF2ToCFF" time="0.067" /><testcase classname="tests.smoke.test_smoke_scripts_CFFToCFF2" name="test_import_scripts_CFFToCFF2" time="0.008" /><testcase classname="tests.smoke.test_smoke_scripts_C_B_D_T_" name="test_import_scripts_C_B_D_T_" time="0.008" /><testcase classname="tests.smoke.test_smoke_scripts_C_B_L_C_" name="test_import_scripts_C_B_L_C_" time="0.011" /><testcase classname="tests.smoke.test_smoke_scripts_C_F_F_" name="test_import_scripts_C_F_F_" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_C_F_F__2" name="test_import_scripts_C_F_F__2" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts_C_O_L_R_" name="test_import_scripts_C_O_L_R_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_C_P_A_L_" name="test_import_scripts_C_P_A_L_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_D_S_I_G_" name="test_import_scripts_D_S_I_G_" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_D__e_b_g" name="test_import_scripts_D__e_b_g" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_DefaultTable" name="test_import_scripts_DefaultTable" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts_E_B_D_T_" name="test_import_scripts_E_B_D_T_" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts_E_B_L_C_" name="test_import_scripts_E_B_L_C_" time="0.000" /><testcase classname="tests.smoke.test_smoke_scripts_ElementInclude" name="test_import_scripts_ElementInclude" time="0.148" /><testcase classname="tests.smoke.test_smoke_scripts_ElementSoup" name="test_import_scripts_ElementSoup" time="0.050" /><testcase classname="tests.smoke.test_smoke_scripts_ElementTree" name="test_import_scripts_ElementTree" time="0.059" /><testcase classname="tests.smoke.test_smoke_scripts_F_F_T_M_" name="test_import_scripts_F_F_T_M_" time="0.023" /><testcase classname="tests.smoke.test_smoke_scripts_F__e_a_t" name="test_import_scripts_F__e_a_t" time="0.010" /><testcase classname="tests.smoke.test_smoke_scripts_G_D_E_F_" name="test_import_scripts_G_D_E_F_" time="0.013" /><testcase classname="tests.smoke.test_smoke_scripts_G_M_A_P_" name="test_import_scripts_G_M_A_P_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_G_P_K_G_" name="test_import_scripts_G_P_K_G_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_G_P_O_S_" name="test_import_scripts_G_P_O_S_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_G_S_U_B_" name="test_import_scripts_G_S_U_B_" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts_G_V_A_R_" name="test_import_scripts_G_V_A_R_" time="0.006" /><testcase classname="tests.smoke.test_smoke_scripts_G__l_a_t" name="test_import_scripts_G__l_a_t" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_G__l_o_c" name="test_import_scripts_G__l_o_c" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_H_V_A_R_" name="test_import_scripts_H_V_A_R_" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_J_S_T_F_" name="test_import_scripts_J_S_T_F_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_L_T_S_H_" name="test_import_scripts_L_T_S_H_" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts_M_A_T_H_" name="test_import_scripts_M_A_T_H_" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts_M_E_T_A_" name="test_import_scripts_M_E_T_A_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_M_V_A_R_" name="test_import_scripts_M_V_A_R_" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_MacRoman" name="test_import_scripts_MacRoman" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_Mirrored" name="test_import_scripts_Mirrored" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_OTTags" name="test_import_scripts_OTTags" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts_O_S_2f_2" name="test_import_scripts_O_S_2f_2" time="0.016" /><testcase classname="tests.smoke.test_smoke_scripts_S_I_N_G_" name="test_import_scripts_S_I_N_G_" time="0.018" /><testcase classname="tests.smoke.test_smoke_scripts_S_T_A_T_" name="test_import_scripts_S_T_A_T_" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts_S_V_G_" name="test_import_scripts_S_V_G_" time="0.006" /><testcase classname="tests.smoke.test_smoke_scripts_S__i_l_f" name="test_import_scripts_S__i_l_f" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_S__i_l_l" name="test_import_scripts_S__i_l_l" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_ScriptExtensions" name="test_import_scripts_ScriptExtensions" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_Scripts" name="test_import_scripts_Scripts" time="0.022" /><testcase classname="tests.smoke.test_smoke_scripts_StandardEncoding" name="test_import_scripts_StandardEncoding" time="0.010" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I_B_" name="test_import_scripts_T_S_I_B_" time="0.008" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I_C_" name="test_import_scripts_T_S_I_C_" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I_D_" name="test_import_scripts_T_S_I_D_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I_J_" name="test_import_scripts_T_S_I_J_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I_P_" name="test_import_scripts_T_S_I_P_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I_S_" name="test_import_scripts_T_S_I_S_" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I_V_" name="test_import_scripts_T_S_I_V_" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I__0" name="test_import_scripts_T_S_I__0" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I__1" name="test_import_scripts_T_S_I__1" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I__2" name="test_import_scripts_T_S_I__2" time="0.007" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I__3" name="test_import_scripts_T_S_I__3" time="0.009" /><testcase classname="tests.smoke.test_smoke_scripts_T_S_I__5" name="test_import_scripts_T_S_I__5" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts_T_T_F_A_" name="test_import_scripts_T_T_F_A_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_TupleVariation" name="test_import_scripts_TupleVariation" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts_V_A_R_C_" name="test_import_scripts_V_A_R_C_" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts_V_D_M_X_" name="test_import_scripts_V_D_M_X_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_V_O_R_G_" name="test_import_scripts_V_O_R_G_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts_V_V_A_R_" name="test_import_scripts_V_V_A_R_" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts___about__" name="test_import_scripts___about__" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts___config__" name="test_import_scripts___config__" time="0.012" /><testcase classname="tests.smoke.test_smoke_scripts___config___2" name="test_import_scripts___config___2" time="0.035" /><testcase classname="tests.smoke.test_smoke_scripts___config___3" name="test_import_scripts___config___3" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts___init__" name="test_import_scripts___init__" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts___main__" name="test_import_scripts___main__" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts___main___10" name="test_import_scripts___main___10" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts___main___11" name="test_import_scripts___main___11" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts___main___12" name="test_import_scripts___main___12" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts___main___13" name="test_import_scripts___main___13" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts___main___14" name="test_import_scripts___main___14" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts___main___15" name="test_import_scripts___main___15" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts___main___16" name="test_import_scripts___main___16" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts___main___17" name="test_import_scripts___main___17" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts___main___18" name="test_import_scripts___main___18" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts___main___19" name="test_import_scripts___main___19" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts___main___2" name="test_import_scripts___main___2" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts___main___20" name="test_import_scripts___main___20" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts___main___21" name="test_import_scripts___main___21" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts___main___22" name="test_import_scripts___main___22" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts___main___3" name="test_import_scripts___main___3" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts___main___4" name="test_import_scripts___main___4" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts___main___5" name="test_import_scripts___main___5" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts___main___6" name="test_import_scripts___main___6" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts___main___7" name="test_import_scripts___main___7" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts___main___8" name="test_import_scripts___main___8" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts___main___9" name="test_import_scripts___main___9" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts___meta__" name="test_import_scripts___meta__" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts___pip_runner__" name="test_import_scripts___pip_runner__" time="0.020" /><testcase classname="tests.smoke.test_smoke_scripts___version__" name="test_import_scripts___version__" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__a_n_k_r" name="test_import_scripts__a_n_k_r" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__a_v_a_r" name="test_import_scripts__a_v_a_r" time="0.235" /><testcase classname="tests.smoke.test_smoke_scripts__abc" name="test_import_scripts__abc" time="0.610" /><testcase classname="tests.smoke.test_smoke_scripts__abnf" name="test_import_scripts__abnf" time="0.013" /><testcase classname="tests.smoke.test_smoke_scripts__adapters" name="test_import_scripts__adapters" time="0.012" /><testcase classname="tests.smoke.test_smoke_scripts__add_docstring" name="test_import_scripts__add_docstring" time="0.006" /><testcase classname="tests.smoke.test_smoke_scripts__add_newdocs" name="test_import_scripts__add_newdocs" time="0.000" /><testcase classname="tests.smoke.test_smoke_scripts__add_newdocs_scalars" name="test_import_scripts__add_newdocs_scalars" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts__api" name="test_import_scripts__api" time="0.006" /><testcase classname="tests.smoke.test_smoke_scripts__api_2" name="test_import_scripts__api_2" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__app" name="test_import_scripts__app" time="0.062" /><testcase classname="tests.smoke.test_smoke_scripts__appengine_environ" name="test_import_scripts__appengine_environ" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__apply_pyprojecttoml" name="test_import_scripts__apply_pyprojecttoml" time="1.462" /><testcase classname="tests.smoke.test_smoke_scripts__array_like" name="test_import_scripts__array_like" time="0.007" /><testcase classname="tests.smoke.test_smoke_scripts__arrow_utils" name="test_import_scripts__arrow_utils" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__asarray" name="test_import_scripts__asarray" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts__ast_gen" name="test_import_scripts__ast_gen" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__asymmetric" name="test_import_scripts__asymmetric" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts__asyncgens" name="test_import_scripts__asyncgens" time="0.019" /><testcase classname="tests.smoke.test_smoke_scripts__asyncio" name="test_import_scripts__asyncio" time="0.007" /><testcase classname="tests.smoke.test_smoke_scripts__asyncio_selector_thread" name="test_import_scripts__asyncio_selector_thread" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__b_s_l_n" name="test_import_scripts__b_s_l_n" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__base" name="test_import_scripts__base" time="0.007" /><testcase classname="tests.smoke.test_smoke_scripts__base_connection" name="test_import_scripts__base_connection" time="0.015" /><testcase classname="tests.smoke.test_smoke_scripts__build_config" name="test_import_scripts__build_config" time="0.004" /><testcase classname="tests.smoke.test_smoke_scripts__build_tables" name="test_import_scripts__build_tables" time="0.209" /><testcase classname="tests.smoke.test_smoke_scripts__c_i_d_g" name="test_import_scripts__c_i_d_g" time="0.006" /><testcase classname="tests.smoke.test_smoke_scripts__c_m_a_p" name="test_import_scripts__c_m_a_p" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts__c_v_a_r" name="test_import_scripts__c_v_a_r" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__c_v_t" name="test_import_scripts__c_v_t" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__cached" name="test_import_scripts__cached" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__cachedmethod" name="test_import_scripts__cachedmethod" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__callable" name="test_import_scripts__callable" time="0.005" /><testcase classname="tests.smoke.test_smoke_scripts__catch" name="test_import_scripts__catch" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__cell_widths" name="test_import_scripts__cell_widths" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__channel" name="test_import_scripts__channel" time="0.006" /><testcase classname="tests.smoke.test_smoke_scripts__char_codes" name="test_import_scripts__char_codes" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__check_streams" name="test_import_scripts__check_streams" time="0.032" /><testcase classname="tests.smoke.test_smoke_scripts__checkpoints" name="test_import_scripts__checkpoints" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts__cipheralgorithm" name="test_import_scripts__cipheralgorithm" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__cli" name="test_import_scripts__cli" time="0.232" /><testcase classname="tests.smoke.test_smoke_scripts__cmd" name="test_import_scripts__cmd" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__cmp" name="test_import_scripts__cmp" time="0.009" /><testcase classname="tests.smoke.test_smoke_scripts__cmp_2" name="test_import_scripts__cmp_2" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__collections" name="test_import_scripts__collections" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__color_data" name="test_import_scripts__color_data" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__common" name="test_import_scripts__common" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__common_2" name="test_import_scripts__common_2" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__common_3" name="test_import_scripts__common_3" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__common_4" name="test_import_scripts__common_4" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__compat" name="test_import_scripts__compat" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts__compat_2" name="test_import_scripts__compat_2" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__compat_3" name="test_import_scripts__compat_3" time="0.009" /><testcase classname="tests.smoke.test_smoke_scripts__concat_tb" name="test_import_scripts__concat_tb" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__conditional" name="test_import_scripts__conditional" time="0.002" /><testcase classname="tests.smoke.test_smoke_scripts__config" name="test_import_scripts__config" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts__config_2" name="test_import_scripts__config_2" time="0.038" /><testcase classname="tests.smoke.test_smoke_scripts__config_3" name="test_import_scripts__config_3" time="0.071" /><testcase classname="tests.smoke.test_smoke_scripts__config_4" name="test_import_scripts__config_4" time="0.008" /><testcase classname="tests.smoke.test_smoke_scripts__configtool" name="test_import_scripts__configtool" time="0.006" /><testcase classname="tests.smoke.test_smoke_scripts__connection" name="test_import_scripts__connection" time="0.051" /><testcase classname="tests.smoke.test_smoke_scripts__content" name="test_import_scripts__content" time="0.022" /><testcase classname="tests.smoke.test_smoke_scripts__cookiejar" name="test_import_scripts__cookiejar" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts__core" name="test_import_scripts__core" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts__cov_probe" name="test_import_scripts__cov_probe" time="0.001" /><testcase classname="tests.smoke.test_smoke_scripts__datasource" name="test_import_scripts__datasource" time="0.033" /><testcase classname="tests.smoke.test_smoke_scripts__decorators" name="test_import_scripts__decorators" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__deprecate" name="test_import_scripts__deprecate" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__deprecation" name="test_import_scripts__deprecation" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__deprecation_2" name="test_import_scripts__deprecation_2" time="0.003" /><testcase classname="tests.smoke.test_smoke_scripts__deprecation_warning" name="test_import_scripts__deprecation_warning" time="0.018" /><testcase classname="tests.smoke.test_smoke_scripts__dfi_types" name="test_import_scripts__dfi_types" time="0.014" /><testcase classname="tests.smoke.test_smoke_scripts__diffcommand" name="test_import_scripts__diffcommand" time="0.176"><failure message="TypeError: 'str' object is not callable">def test_import_scripts__diffcommand():
&gt;       mod = importlib.import_module("scripts._diffcommand")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\smoke\test_smoke_scripts__diffcommand.py:5: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
C:\Users\HP\AppData\Local\Programs\Python\Python311\Lib\importlib\__init__.py:126: in import_module
    return _bootstrap._gcd_import(name[level:], package, level)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
&lt;frozen importlib._bootstrap&gt;:1204: in _gcd_import
    ???
&lt;frozen importlib._bootstrap&gt;:1176: in _find_and_load
    ???
&lt;frozen importlib._bootstrap&gt;:1147: in _find_and_load_unlocked
    ???
&lt;frozen importlib._bootstrap&gt;:690: in _load_unlocked
    ???
&lt;frozen importlib._bootstrap_external&gt;:940: in exec_module
    ???
&lt;frozen importlib._bootstrap&gt;:241: in _call_with_frames_removed
    ???
scripts\_diffcommand.py:5: in &lt;module&gt;
    from .diff import htmldiff
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

    # cython: language_level=3
    
    try:
        import cython
    except ImportError:
    
        class fake_cython:
            compiled = False
    
            def cfunc(self, func):
                return func
    
            def cclass(self, func):
                return func
    
            def declare(self, _, value):
                return value
    
            def __getattr__(self, type_name):
                return "object"
    
        cython = fake_cython()
    
    try:
        from . import _difflib as difflib
        import inspect
    
        if inspect.isfunction(difflib.get_close_matches):
            raise ImportError(
                "Embedded difflib is not compiled to a fast binary, using the stdlib instead."
            )
        from cython.cimports.lxml.html._difflib import SequenceMatcher
    except ImportError:
        import difflib
    
        if not cython.compiled:
            from difflib import SequenceMatcher
    
    import itertools
    import functools
    import operator
    import re
    
    from lxml import etree
    from lxml.html import fragment_fromstring
    from . import defs
    
    __all__ = ["html_annotate", "htmldiff"]
    
    group_by_first_item = functools.partial(itertools.groupby, key=operator.itemgetter(0))
    
    ############################################################
    ## Annotation
    ############################################################
    
    
    @cython.cfunc
    def html_escape(
        text: str, _escapes: tuple = ("&amp;amp;", "&amp;lt;", "&amp;gt;", "&amp;quot;", "&amp;#x27;")
    ) -&gt; str:
        # Not so slow compiled version of 'html.escape()'.
        # Most of the time, we replace little to nothing, so use a fast decision what needs to be done.
        ch: cython.Py_UCS4
        replace: cython.char[5] = [False] * 5
        for ch in text:
            replace[0] |= ch == "&amp;"
            replace[1] |= ch == "&lt;"
            replace[2] |= ch == "&gt;"
            replace[3] |= ch == '"'
            replace[4] |= ch == "'"
    
        for i in range(5):
            if replace[i]:
                text = text.replace("&amp;&lt;&gt;\"'"[i], _escapes[i])
    
        return text
    
    
    if not cython.compiled:
        from html import escape as html_escape
    
    
    def default_markup(text, version):
        return '&lt;span title="%s"&gt;%s&lt;/span&gt;' % (html_escape(version), text)
    
    
    def html_annotate(doclist, markup=default_markup):
        """
        doclist should be ordered from oldest to newest, like::
    
            &gt;&gt;&gt; version1 = 'Hello World'
            &gt;&gt;&gt; version2 = 'Goodbye World'
            &gt;&gt;&gt; print(html_annotate([(version1, 'version 1'),
            ...                      (version2, 'version 2')]))
            &lt;span title="version 2"&gt;Goodbye&lt;/span&gt; &lt;span title="version 1"&gt;World&lt;/span&gt;
    
        The documents must be *fragments* (str/UTF8 or unicode), not
        complete documents
    
        The markup argument is a function to markup the spans of words.
        This function is called like markup('Hello', 'version 2'), and
        returns HTML.  The first argument is text and never includes any
        markup.  The default uses a span with a title:
    
            &gt;&gt;&gt; print(default_markup('Some Text', 'by Joe'))
            &lt;span title="by Joe"&gt;Some Text&lt;/span&gt;
        """
        # The basic strategy we have is to split the documents up into
        # logical tokens (which are words with attached markup).  We then
        # do diffs of each of the versions to track when a token first
        # appeared in the document; the annotation attached to the token
        # is the version where it first appeared.
        tokenlist = [tokenize_annotated(doc, version) for doc, version in doclist]
        cur_tokens = tokenlist[0]
        for tokens in tokenlist[1:]:
            html_annotate_merge_annotations(cur_tokens, tokens)
            cur_tokens = tokens
    
        # After we've tracked all the tokens, we can combine spans of text
        # that are adjacent and have the same annotation
        cur_tokens = compress_tokens(cur_tokens)
        # And finally add markup
        result = markup_serialize_tokens(cur_tokens, markup)
        return "".join(result).strip()
    
    
    def tokenize_annotated(doc, annotation):
        """Tokenize a document and add an annotation attribute to each token"""
        tokens = tokenize(doc, include_hrefs=False)
        for tok in tokens:
            tok.annotation = annotation
        return tokens
    
    
    def html_annotate_merge_annotations(tokens_old, tokens_new):
        """Merge the annotations from tokens_old into tokens_new, when the
        tokens in the new document already existed in the old document.
        """
        s = InsensitiveSequenceMatcher(a=tokens_old, b=tokens_new)
        commands = s.get_opcodes()
    
        for command, i1, i2, j1, j2 in commands:
            if command == "equal":
                eq_old = tokens_old[i1:i2]
                eq_new = tokens_new[j1:j2]
                copy_annotations(eq_old, eq_new)
    
    
    def copy_annotations(src, dest):
        """
        Copy annotations from the tokens listed in src to the tokens in dest
        """
        assert len(src) == len(dest)
        for src_tok, dest_tok in zip(src, dest):
            dest_tok.annotation = src_tok.annotation
    
    
    def compress_tokens(tokens):
        """
        Combine adjacent tokens when there is no HTML between the tokens,
        and they share an annotation
        """
        result = [tokens[0]]
        for tok in tokens[1:]:
            if (
                not tok.pre_tags
                and not result[-1].post_tags
                and result[-1].annotation == tok.annotation
            ):
                compress_merge_back(result, tok)
            else:
                result.append(tok)
        return result
    
    
    @cython.cfunc
    def compress_merge_back(tokens: list, tok):
        """Merge tok into the last element of tokens (modifying the list of
        tokens in-place)."""
        last = tokens[-1]
        if type(last) is not token or type(tok) is not token:
            tokens.append(tok)
        else:
            text = last + last.trailing_whitespace + tok
            merged = token(
                text,
                pre_tags=last.pre_tags,
                post_tags=tok.post_tags,
                trailing_whitespace=tok.trailing_whitespace,
            )
            merged.annotation = last.annotation
            tokens[-1] = merged
    
    
    def markup_serialize_tokens(tokens, markup_func):
        """
        Serialize the list of tokens into a list of text chunks, calling
        markup_func around text to add annotations.
        """
        for token in tokens:
            yield from token.pre_tags
            html = token.html()
            html = markup_func(html, token.annotation) + token.trailing_whitespace
            yield html
            yield from token.post_tags
    
    
    ############################################################
    ## HTML Diffs
    ############################################################
    
    
    def htmldiff(old_html, new_html):
        ## FIXME: this should take parsed documents too, and use their body
        ## or other content.
        """Do a diff of the old and new document.  The documents are HTML
        *fragments* (str/UTF8 or unicode), they are not complete documents
        (i.e., no &lt;html&gt; tag).
    
        Returns HTML with &lt;ins&gt; and &lt;del&gt; tags added around the
        appropriate text.
    
        Markup is generally ignored, with the markup from new_html
        preserved, and possibly some markup from old_html (though it is
        considered acceptable to lose some of the old markup).  Only the
        words in the HTML are diffed.  The exception is &lt;img&gt; tags, which
        are treated like words, and the href attribute of &lt;a&gt; tags, which
        are noted inside the tag itself when there are changes.
        """
        old_html_tokens = tokenize(old_html)
        new_html_tokens = tokenize(new_html)
        result = htmldiff_tokens(old_html_tokens, new_html_tokens)
        try:
            result = "".join(result).strip()
        except (ValueError, TypeError) as exc:
            print(exc)
            result = ""
        return fixup_ins_del_tags(result)
    
    
    def htmldiff_tokens(html1_tokens, html2_tokens):
        """Does a diff on the tokens themselves, returning a list of text
        chunks (not tokens).
        """
        # There are several passes as we do the differences.  The tokens
        # isolate the portion of the content we care to diff; difflib does
        # all the actual hard work at that point.
        #
        # Then we must create a valid document from pieces of both the old
        # document and the new document.  We generally prefer to take
        # markup from the new document, and only do a best effort attempt
        # to keep markup from the old document; anything that we can't
        # resolve we throw away.  Also we try to put the deletes as close
        # to the location where we think they would have been -- because
        # we are only keeping the markup from the new document, it can be
        # fuzzy where in the new document the old text would have gone.
        # Again we just do a best effort attempt.
        s = InsensitiveSequenceMatcher(a=html1_tokens, b=html2_tokens)
        commands = s.get_opcodes()
        result = []
        for command, i1, i2, j1, j2 in commands:
            if command == "equal":
                result.extend(expand_tokens(html2_tokens[j1:j2], equal=True))
                continue
            if command == "insert" or command == "replace":
                ins_tokens = expand_tokens(html2_tokens[j1:j2])
                merge_insert(ins_tokens, result)
            if command == "delete" or command == "replace":
                del_tokens = expand_tokens(html1_tokens[i1:i2])
                merge_delete(del_tokens, result)
    
        # If deletes were inserted directly as &lt;del&gt; then we'd have an
        # invalid document at this point.  Instead we put in special
        # markers, and when the complete diffed document has been created
        # we try to move the deletes around and resolve any problems.
        cleanup_delete(result)
    
        return result
    
    
    def expand_tokens(tokens, equal=False):
        """Given a list of tokens, return a generator of the chunks of
        text for the data in the tokens.
        """
        for token in tokens:
            yield from token.pre_tags
            if not equal or not token.hide_when_equal:
                yield token.html() + token.trailing_whitespace
            yield from token.post_tags
    
    
    def merge_insert(ins_chunks, doc: list):
        """doc is the already-handled document (as a list of text chunks);
        here we add &lt;ins&gt;ins_chunks&lt;/ins&gt; to the end of that."""
        # Though we don't throw away unbalanced start/end tags
        # (we assume there is accompanying markup later or earlier in the
        # document), we only put &lt;ins&gt; around the balanced portion.
    
        # Legacy note: We make a choice here. Originally, we merged all sequences of
        # unbalanced tags together into separate start and end tag groups. Now, we look at
        # each sequence separately, leading to more fine-grained diffs but different
        # tag structure than before.
    
        item: tuple
        for balanced, marked_chunks in group_by_first_item(mark_unbalanced(ins_chunks)):
            chunks = [item[1] for item in marked_chunks]
            if balanced == "b":
                if doc and not doc[-1].endswith(" "):
                    # Fix up the case where the word before the insert didn't end with a space.
                    doc[-1] += " "
                doc.append("&lt;ins&gt;")
                doc.extend(chunks)
                if doc[-1].endswith(" "):
                    # We move space outside of &lt;/ins&gt;.
                    doc[-1] = doc[-1][:-1]
                doc.append("&lt;/ins&gt; ")
            else:
                # unmatched start or end
                doc.extend(chunks)
    
    
    @cython.cfunc
    def tag_name_of_chunk(chunk: str) -&gt; str:
        i: cython.Py_ssize_t
        ch: cython.Py_UCS4
    
        if chunk[0] != "&lt;":
            return ""
    
        start_pos = 1
        for i, ch in enumerate(chunk):
            if ch == "/":
                start_pos = 2
            elif ch == "&gt;":
                return chunk[start_pos:i]
            elif ch.isspace():
                return chunk[start_pos:i]
    
        return chunk[start_pos:]
    
    
    if not cython.compiled:
        # Avoid performance regression in Python due to string iteration.
        def tag_name_of_chunk(chunk: str) -&gt; str:
            return chunk.split(None, 1)[0].strip("&lt;&gt;/")
    
    
    # These are sentinels to represent the start and end of a &lt;del&gt;
    # segment, until we do the cleanup phase to turn them into proper
    # markup:
    class DEL_START:
        pass
    
    
    class DEL_END:
        pass
    
    
    def merge_delete(del_chunks, doc: list):
        """Adds the text chunks in del_chunks to the document doc (another
        list of text chunks) with marker to show it is a delete.
        cleanup_delete later resolves these markers into &lt;del&gt; tags."""
    
        doc.append(DEL_START)
        doc.extend(del_chunks)
        doc.append(DEL_END)
    
    
    def cleanup_delete(chunks: list):
        """Cleans up any DEL_START/DEL_END markers in the document, replacing
        them with &lt;del&gt;&lt;/del&gt;.  To do this while keeping the document
        valid, it may need to drop some tags (either start or end tags).
    
        It may also move the del into adjacent tags to try to move it to a
        similar location where it was originally located (e.g., moving a
        delete into preceding &lt;div&gt; tag, if the del looks like (DEL_START,
        'Text&lt;/div&gt;', DEL_END)
        """
        chunk_count = len(chunks)
    
        i: cython.Py_ssize_t
        del_start: cython.Py_ssize_t
        del_end: cython.Py_ssize_t
        shift_start_right: cython.Py_ssize_t
        shift_end_left: cython.Py_ssize_t
        unbalanced_start: cython.Py_ssize_t
        unbalanced_end: cython.Py_ssize_t
        pos: cython.Py_ssize_t
        start_pos: cython.Py_ssize_t
        chunk: str
    
        start_pos = 0
        while 1:
            # Find a pending DEL_START/DEL_END, splitting the document
            # into stuff-preceding-DEL_START, stuff-inside, and
            # stuff-following-DEL_END
            try:
                del_start = chunks.index(DEL_START, start_pos)
            except ValueError:
                # Nothing found, we've cleaned up the entire doc
                break
            else:
                del_end = chunks.index(DEL_END, del_start + 1)
    
            shift_end_left = shift_start_right = 0
            unbalanced_start = unbalanced_end = 0
            deleted_chunks = mark_unbalanced(chunks[del_start + 1 : del_end])
    
            # For unbalanced start tags at the beginning, find matching (non-deleted)
            # end tags after the current DEL_END and move the start tag outside.
            for balanced, del_chunk in deleted_chunks:
                if balanced != "us":
                    break
                unbalanced_start += 1
                unbalanced_start_name = tag_name_of_chunk(del_chunk)
                for i in range(del_end + 1, chunk_count):
                    if chunks[i] is DEL_START:
                        break
                    chunk = chunks[i]
                    if chunk[0] != "&lt;" or chunk[1] == "/":
                        # Reached a word or closing tag.
                        break
                    name = tag_name_of_chunk(chunk)
                    if name == "ins":
                        # Cannot move into an insert.
                        break
                    assert name != "del", f"Unexpected delete tag: {chunk!r}"
                    if name != unbalanced_start_name:
                        # Avoid mixing in other start tags.
                        break
                    # Exclude start tag to balance the end tag.
                    shift_start_right += 1
    
            # For unbalanced end tags at the end, find matching (non-deleted)
            # start tags before the currend DEL_START and move the end tag outside.
            for balanced, del_chunk in reversed(deleted_chunks):
                if balanced != "ue":
                    break
                unbalanced_end += 1
                unbalanced_end_name = tag_name_of_chunk(del_chunk)
                for i in range(del_start - 1, -1, -1):
                    if chunks[i] is DEL_END:
                        break
                    chunk = chunks[i]
                    if chunk[0] == "&lt;" and chunk[1] != "/":
                        # Reached an opening tag, can we go further?  Maybe not...
                        break
                    name = tag_name_of_chunk(chunk)
                    if name == "ins" or name == "del":
                        # Cannot move into an insert or delete.
                        break
                    if name != unbalanced_end_name:
                        # Avoid mixing in other start tags.
                        break
                    # Exclude end tag to balance the start tag.
                    shift_end_left += 1
    
            """
            # This is what we do below in loops, spelled out using slicing and list copying:
    
            chunks[del_start - shift_end_left : del_end + shift_start_right + 1] = [
                *chunks[del_start + 1: del_start + shift_start_right + 1],
                '&lt;del&gt;',
                *chunks[del_start + unbalanced_start + 1 : del_end - unbalanced_end],
                '&lt;/del&gt; ',
                *chunks[del_end - shift_end_left: del_end],
            ]
    
            new_del_end = del_end - 2 * shift_end_left
            assert chunks[new_del_end] == '&lt;/del&gt; '
            del_end = new_del_end
    
            if new_del_start &gt; 0 and not chunks[new_del_start - 1].endswith(' '):
                # Fix up case where the word before us didn't have a trailing space.
                chunks[new_del_start - 1] += ' '
            if new_del_end &gt; 0 and chunks[new_del_end - 1].endswith(' '):
                # Move space outside of &lt;/del&gt;.
                chunks[new_del_end - 1] = chunks[new_del_end - 1][:-1]
            """
            pos = del_start - shift_end_left
            # Move re-balanced start tags before the '&lt;del&gt;'.
            for i in range(del_start + 1, del_start + shift_start_right + 1):
                chunks[pos] = chunks[i]
                pos += 1
            if pos and not chunks[pos - 1].endswith(" "):
                # Fix up the case where the word before '&lt;del&gt;' didn't have a trailing space.
                chunks[pos - 1] += " "
            chunks[pos] = "&lt;del&gt;"
            pos += 1
            # Copy only the balanced deleted content between '&lt;del&gt;' and '&lt;/del&gt;'.
            for i in range(del_start + unbalanced_start + 1, del_end - unbalanced_end):
                chunks[pos] = chunks[i]
                pos += 1
            if chunks[pos - 1].endswith(" "):
                # Move trailing space outside of &lt;/del&gt;.
                chunks[pos - 1] = chunks[pos - 1][:-1]
            chunks[pos] = "&lt;/del&gt; "
            pos += 1
            # Move re-balanced end tags after the '&lt;/del&gt;'.
            for i in range(del_end - shift_end_left, del_end):
                chunks[pos] = chunks[i]
                pos += 1
            # Adjust the length of the processed part in 'chunks'.
            del chunks[pos : del_end + shift_start_right + 1]
            start_pos = pos
    
    
    @cython.cfunc
    def mark_unbalanced(chunks) -&gt; list:
        tag_stack = []
        marked = []
    
        chunk: str
        parents: list
    
        for chunk in chunks:
            if not chunk.startswith("&lt;"):
                marked.append(("b", chunk))
                continue
    
            name = tag_name_of_chunk(chunk)
            if name in empty_tags:
                marked.append(("b", chunk))
                continue
    
            if chunk[1] == "/":
                # closing tag found, unwind tag stack
                while tag_stack:
                    start_name, start_chunk, parents = tag_stack.pop()
                    if start_name == name:
                        # balanced tag closing, keep rest of stack intact
                        parents.append(("b", start_chunk))
                        parents.extend(marked)
                        parents.append(("b", chunk))
                        marked = parents
                        chunk = None
                        break
                    else:
                        # unmatched start tag
                        parents.append(("us", start_chunk))
                        parents.extend(marked)
                        marked = parents
    
                if chunk is not None:
                    # unmatched end tag left after clearing the stack
                    marked.append(("ue", chunk))
            else:
                # new start tag found
                tag_stack.append((name, chunk, marked))
                marked = []
    
        # add any unbalanced start tags
        while tag_stack:
            _, start_chunk, parents = tag_stack.pop()
            parents.append(("us", start_chunk))
            parents.extend(marked)
            marked = parents
    
        return marked
    
    
    class token(str):
        """Represents a diffable token, generally a word that is displayed to
        the user.  Opening tags are attached to this token when they are
        adjacent (pre_tags) and closing tags that follow the word
        (post_tags).  Some exceptions occur when there are empty tags
        adjacent to a word, so there may be close tags in pre_tags, or
        open tags in post_tags.
    
        We also keep track of whether the word was originally followed by
        whitespace, even though we do not want to treat the word as
        equivalent to a similar word that does not have a trailing
        space."""
    
        # When this is true, the token will be eliminated from the
        # displayed diff if no change has occurred:
        hide_when_equal = False
    
        def __new__(cls, text, pre_tags=None, post_tags=None, trailing_whitespace=""):
            obj = str.__new__(cls, text)
    
            obj.pre_tags = pre_tags if pre_tags is not None else []
            obj.post_tags = post_tags if post_tags is not None else []
            obj.trailing_whitespace = trailing_whitespace
    
            return obj
    
        def __repr__(self):
            return "token(%s, %r, %r, %r)" % (
                str.__repr__(self),
                self.pre_tags,
                self.post_tags,
                self.trailing_whitespace,
            )
    
        def html(self):
            return str(self)
    
    
    class tag_token(token):
        """Represents a token that is actually a tag.  Currently this is just
        the &lt;img&gt; tag, which takes up visible space just like a word but
        is only represented in a document by a tag."""
    
        def __new__(
            cls, tag, data, html_repr, pre_tags=None, post_tags=None, trailing_whitespace=""
        ):
            obj = token.__new__(
                cls,
                f"{type}: {data}",
                pre_tags=pre_tags,
                post_tags=post_tags,
                trailing_whitespace=trailing_whitespace,
            )
            obj.tag = tag
            obj.data = data
            obj.html_repr = html_repr
            return obj
    
        def __repr__(self):
            return (
                "tag_token(%s, %s, html_repr=%s, post_tags=%r, pre_tags=%r, trailing_whitespace=%r)"
                % (
                    self.tag,
                    self.data,
                    self.html_repr,
                    self.pre_tags,
                    self.post_tags,
                    self.trailing_whitespace,
                )
            )
    
        def html(self):
            return self.html_repr
    
    
    class href_token(token):
        """Represents the href in an anchor tag.  Unlike other words, we only
        show the href when it changes."""
    
        hide_when_equal = True
    
        def html(self):
            return " Link: %s" % self
    
    
    def tokenize(html, include_hrefs=True):
        """
        Parse the given HTML and returns token objects (words with attached tags).
    
        This parses only the content of a page; anything in the head is
        ignored, and the &lt;head&gt; and &lt;body&gt; elements are themselves
        optional.  The content is then parsed by lxml, which ensures the
        validity of the resulting parsed document (though lxml may make
        incorrect guesses when the markup is particular bad).
    
        &lt;ins&gt; and &lt;del&gt; tags are also eliminated from the document, as
        that gets confusing.
    
        If include_hrefs is true, then the href attribute of &lt;a&gt; tags is
        included as a special kind of diffable token."""
        if etree.iselement(html):
            body_el = html
        else:
            body_el = parse_html(html, cleanup=True)
        # Then we split the document into text chunks for each tag, word, and end tag:
        chunks = flatten_el(body_el, skip_tag=True, include_hrefs=include_hrefs)
        # Finally re-joining them into token objects:
        return fixup_chunks(chunks)
    
    
    def parse_html(html, cleanup=True):
        """
        Parses an HTML fragment, returning an lxml element.  Note that the HTML will be
        wrapped in a &lt;div&gt; tag that was not in the original document.
    
        If cleanup is true, make sure there's no &lt;head&gt; or &lt;body&gt;, and get
        rid of any &lt;ins&gt; and &lt;del&gt; tags.
        """
        if cleanup:
            # This removes any extra markup or structure like &lt;head&gt;:
            html = cleanup_html(html)
        return fragment_fromstring(html, create_parent=True)
    
    
    _search_body = re.compile(r"&lt;body.*?&gt;", re.I | re.S).search
    _search_end_body = re.compile(r"&lt;/body.*?&gt;", re.I | re.S).search
    _replace_ins_del = re.compile(r"&lt;/?(ins|del).*?&gt;", re.I | re.S).sub
    
    
    def cleanup_html(html):
        """This 'cleans' the HTML, meaning that any page structure is removed
        (only the contents of &lt;body&gt; are used, if there is any &lt;body).
        Also &lt;ins&gt; and &lt;del&gt; tags are removed."""
        match = _search_body(html)
        if match:
            html = html[match.end() :]
        match = _search_end_body(html)
        if match:
            html = html[: match.start()]
        html = _replace_ins_del("", html)
        return html
    
    
    def split_trailing_whitespace(word):
        """
        This function takes a word, such as 'test\n\n' and returns ('test','\n\n')
        """
        stripped_length = len(word.rstrip())
        return word[0:stripped_length], word[stripped_length:]
    
    
    def fixup_chunks(chunks):
        """
        This function takes a list of chunks and produces a list of tokens.
        """
        tag_accum = []
        cur_word = None
        result = []
        for chunk in chunks:
            if isinstance(chunk, tuple):
                if chunk[0] == "img":
                    src = chunk[1]
                    tag, trailing_whitespace = split_trailing_whitespace(chunk[2])
                    cur_word = tag_token(
                        "img",
                        src,
                        html_repr=tag,
                        pre_tags=tag_accum,
                        trailing_whitespace=trailing_whitespace,
                    )
                    tag_accum = []
                    result.append(cur_word)
    
                elif chunk[0] == "href":
                    href = chunk[1]
                    cur_word = href_token(href, pre_tags=tag_accum, trailing_whitespace=" ")
                    tag_accum = []
                    result.append(cur_word)
                continue
    
            if is_word(chunk):
                chunk, trailing_whitespace = split_trailing_whitespace(chunk)
                cur_word = token(
                    chunk, pre_tags=tag_accum, trailing_whitespace=trailing_whitespace
                )
                tag_accum = []
                result.append(cur_word)
    
            elif is_start_tag(chunk):
                tag_accum.append(chunk)
    
            elif is_end_tag(chunk):
                if tag_accum:
                    tag_accum.append(chunk)
                else:
                    assert (
                        cur_word
                    ), "Weird state, cur_word=%r, result=%r, chunks=%r of %r" % (
                        cur_word,
                        result,
                        chunk,
                        chunks,
                    )
                    cur_word.post_tags.append(chunk)
            else:
                assert False
    
        if not result:
            return [token("", pre_tags=tag_accum)]
        else:
            result[-1].post_tags.extend(tag_accum)
    
        return result
    
    
    # All the tags in HTML that don't require end tags:
    empty_tags = cython.declare(frozenset, defs.empty_tags)
    
    block_level_tags = cython.declare(
        frozenset,
        frozenset(
            [
                "address",
                "blockquote",
                "center",
                "dir",
                "div",
                "dl",
                "fieldset",
                "form",
                "h1",
                "h2",
                "h3",
                "h4",
                "h5",
                "h6",
                "hr",
                "isindex",
                "menu",
                "noframes",
                "noscript",
                "ol",
                "p",
                "pre",
                "table",
                "ul",
            ]
        ),
    )
    
    block_level_container_tags = cython.declare(
        frozenset,
        frozenset(
            [
                "dd",
                "dt",
                "frameset",
                "li",
                "tbody",
                "td",
                "tfoot",
                "th",
                "thead",
                "tr",
            ]
        ),
    )
    
    any_block_level_tag = cython.declare(
        tuple, tuple(sorted(block_level_tags | block_level_container_tags))
    )
    
    
    def flatten_el(el, include_hrefs, skip_tag=False):
        """Takes an lxml element el, and generates all the text chunks for
        that tag.  Each start tag is a chunk, each word is a chunk, and each
        end tag is a chunk.
    
        If skip_tag is true, then the outermost container tag is
        not returned (just its contents)."""
        if not skip_tag:
            if el.tag == "img":
                yield ("img", el.get("src"), start_tag(el))
            else:
                yield start_tag(el)
        if el.tag in empty_tags and not el.text and not len(el) and not el.tail:
            return
        start_words = split_words(el.text)
        for word in start_words:
            yield html_escape(word)
        for child in el:
            yield from flatten_el(child, include_hrefs=include_hrefs)
        if el.tag == "a" and el.get("href") and include_hrefs:
            yield ("href", el.get("href"))
        if not skip_tag:
            yield end_tag(el)
            end_words = split_words(el.tail)
            for word in end_words:
                yield html_escape(word)
    
    
    _find_words = re.compile(r"\S+(?:\s+|$)", re.U).findall
    
    
    def split_words(text):
        """Splits some text into words. Includes trailing whitespace
        on each word when appropriate."""
        if not text or not text.strip():
            return []
    
        words = _find_words(text)
        return words
    
    
    _has_start_whitespace = re.compile(r"^[ \t\n\r]").match
    
    
    def start_tag(el):
        """
        The text representation of the start tag for a tag.
        """
        attributes = "".join(
            [f' {name}="{html_escape(value)}"' for name, value in el.attrib.items()]
        )
        return f"&lt;{el.tag}{attributes}&gt;"
    
    
    def end_tag(el):
        """The text representation of an end tag for a tag.  Includes
        trailing whitespace when appropriate."""
        tail = el.tail
        extra = " " if tail and _has_start_whitespace(tail) else ""
        return f"&lt;/{el.tag}&gt;{extra}"
    
    
    def is_word(tok):
        return not tok.startswith("&lt;")
    
    
    def is_end_tag(tok):
        return tok.startswith("&lt;/")
    
    
    def is_start_tag(tok):
        return tok.startswith("&lt;") and not tok.startswith("&lt;/")
    
    
    def fixup_ins_del_tags(html):
        """Given an html string, move any &lt;ins&gt; or &lt;del&gt; tags inside of any
        block-level elements, e.g. transform &lt;ins&gt;&lt;p&gt;word&lt;/p&gt;&lt;/ins&gt; to
        &lt;p&gt;&lt;ins&gt;word&lt;/ins&gt;&lt;/p&gt;"""
        doc = parse_html(html, cleanup=False)
        _fixup_ins_del_tags(doc)
        html = serialize_html_fragment(doc, skip_outer=True)
        return html
    
    
    def serialize_html_fragment(el, skip_outer=False):
        """Serialize a single lxml element as HTML.  The serialized form
        includes the elements tail.
    
        If skip_outer is true, then don't serialize the outermost tag
        """
        assert not isinstance(
            el, str
        ), f"You should pass in an element, not a string like {el!r}"
        html = etree.tostring(el, method="html", encoding="unicode")
        if skip_outer:
            # Get rid of the extra starting tag:
            html = html[html.find("&gt;") + 1 :]
            # Get rid of the extra end tag:
            html = html[: html.rfind("&lt;")]
            return html.strip()
        else:
            return html
    
    
    @cython.cfunc
    def _fixup_ins_del_tags(doc):
        """fixup_ins_del_tags that works on an lxml document in-place"""
        for el in list(doc.iter("ins", "del")):
            if not _contains_block_level_tag(el):
                continue
            _move_el_inside_block(el, tag=el.tag)
            el.drop_tag()
            # _merge_element_contents(el)
    
    
    @cython.cfunc
    def _contains_block_level_tag(el):
        """True if the element contains any block-level elements, like &lt;p&gt;, &lt;td&gt;, etc."""
        for el in el.iter(*any_block_level_tag):
            return True
        return False
    
    
    @cython.cfunc
    def _move_el_inside_block(el, tag):
        """helper for _fixup_ins_del_tags; actually takes the &lt;ins&gt; etc tags
        and moves them inside any block-level tags."""
        makeelement = el.makeelement
        for block_level_el in el.iter(*any_block_level_tag):
            if block_level_el is not el:
                break
        else:
            # No block-level tags in any child
            children_tag = makeelement(tag)
            children_tag.text = el.text
            el.text = None
            children_tag.extend(iter(el))
            el[:] = [children_tag]
            return
    
        for child in list(el):
            if _contains_block_level_tag(child):
                _move_el_inside_block(child, tag)
                if child.tail:
                    tail_tag = makeelement(tag)
                    tail_tag.text = child.tail
                    child.tail = None
                    child.addnext(tail_tag)
            else:
                child_tag = makeelement(tag)
                el.replace(child, child_tag)
                child_tag.append(child)
        if el.text:
            text_tag = makeelement(tag)
            text_tag.text = el.text
            el.text = None
            el.insert(0, text_tag)
    
    
    def _merge_element_contents(el):
        """
        Removes an element, but merges its contents into its place, e.g.,
        given &lt;p&gt;Hi &lt;i&gt;there!&lt;/i&gt;&lt;/p&gt;, if you remove the &lt;i&gt; element you get
        &lt;p&gt;Hi there!&lt;/p&gt;
        """
        parent = el.getparent()
        text = el.text
        tail = el.tail
        if tail:
            if not len(el):
                text = (text or "") + tail
            else:
                el[-1].tail = (el[-1].tail or "") + tail
        index = parent.index(el)
        if text:
            previous = el.getprevious()
            if previous is None:
                parent.text = (parent.text or "") + text
            else:
                previous.tail = (previous.tail or "") + text
        parent[index : index + 1] = el.getchildren()
    
    
&gt;   @cython.final
     ^^^^^^^^^^^^
E   TypeError: 'str' object is not callable

scripts\diff.py:1018: TypeError</failure></testcase></testsuite></testsuites>
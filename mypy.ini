[mypy]
python_version = 3.13
ignore_missing_imports = True

# Exclude every *file* that is not under scripts/phase11
# Important: limit to files with .py/.pyi so directories aren't excluded
exclude = (?x)^(?!.*scripts[/\\]phase11[/\\]).*\.pyi?$
[mypy]
python_version = 3.13
ignore_missing_imports = True

# CI calls "mypy ."; this regex applies to absolute paths.
# Keep ONLY files whose path contains scripts/phase11/
exclude = (?x)^(?!.*scripts[/\\]phase11[/\\]).+
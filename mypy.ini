[mypy]
python_version = 3.13
ignore_missing_imports = True

# Exclude every path that does NOT start with scripts/phase11/
# Works on Linux and Windows paths.
exclude = ^(?!scripts[/\\]phase11[/\\]).+
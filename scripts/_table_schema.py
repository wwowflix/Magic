"""MAGIC shim for pandas table schema helpers.

The original implementation depends on internal pandas JSON APIs.
MAGIC only needs this module to import cleanly.
"""

from __future__ import annotations

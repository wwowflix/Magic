from __future__ import annotations

"""
MAGIC stub: safe replacement for an autocomplete web scraper.

The original file contained characters that did not encode under cp1252
on Windows, causing UnicodeEncodeError during import. Here we provide a
minimal stub.
"""


def fetch_suggestions(query: str) -> list[str]:
    """Return an empty suggestion list placeholder."""
    return []

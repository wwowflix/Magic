from __future__ import annotations

"""
MAGIC stub: bokeh_renderer.

The original module required the external "bokeh" package. In the MAGIC
environment we do not ship bokeh, so this stub only keeps the import
surface alive.
"""


def render_with_bokeh(*args, **kwargs):
    """
    Tiny placeholder used only so tests can import this module
    without requiring the real bokeh library.
    """
    return None

from __future__ import annotations

"""
MAGIC stub: base_command.

The original module came from pip internals and depended on a number of
pip._internal symbols. We only need imports to succeed in the MAGIC
vendored layout.
"""


class BaseCommand:
    """Tiny placeholder base command."""

    def main(self, args=None):
        return 0

from __future__ import annotations

"""
MAGIC stub: bdist_egg (legacy distutils/setuptools command).

The real code integrates deeply with setuptools and distutils. For MAGIC
we only provide a small placeholder class so imports succeed.
"""


class bdist_egg:
    """Minimal dummy command for compatibility."""

    def run(self):
        return None
